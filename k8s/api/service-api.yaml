apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: bravo
spec:
  # ClusterIP only — exposed externally via Ingress → frontend Nginx → api:8000
  selector:
    app: api
  ports:
    - port: 8000
      targetPort: 8000
